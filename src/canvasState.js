export const state = [];
export const dayState = [];
export const hourState = [];

export const dragState = {
    dragStart: null,
    startCell: null,
    dragging: false,
    paintSelected: true
};

export const callbacks = {
    onChange: f => f
};
